%h1 All Posts Tagged as "#{@tag}"
- items_with_tag(@tag).select { |item| item[:kind] == 'article' }.sort_by { |item| item[:created_at] }.reverse_each do |article|
  .article-headline= render 'articles/header', :article => article, :headline_tag => 'h2'
